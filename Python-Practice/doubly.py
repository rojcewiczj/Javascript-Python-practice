class Node():
    def __init__(self, data):
        self.data = data
        self.left = None
        self.right = None

def recursive(root):
    print(root)
    if root.left:
        print(left)
        root.left.recursive()
    if root.right:
        print(right)
        root.right.recursive()

new_node = Node(3)
root = new_node
another_node = Node(5)
other_node = Node(2)
lower_node = Node(1)

def insert(node):
   







